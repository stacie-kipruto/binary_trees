#include "binary_trees.h"

/**
 * avl_remove - remove node from AVL tree
 * @root: pointer to root node of the tree
 * @value: value to remove from tree
 * Return: pointer to new root node of tree after removing value and
 * after rebalancing
 */

avl_t *avl_remove(avl_t *root, int value)
{
	printf("%d", root->n);
	printf("%d", value);
	return (NULL);
}
